import { FavoriteCocktailDirective } from './favorite-cocktail.directive';

describe('FavoriteCocktailDirective', () => {
  let directive: FavoriteCocktailDirective;

  beforeEach(() => {
    directive = new FavoriteCocktailDirective();
  });

  it('should create an instance', () => {
    expect(directive).toBeTruthy();
  });
});


